{
  "name": "gacha-health-webhook",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Cloudflare Worker: receives Discord slash-command interactions and dispatches the matching GitHub Actions workflow",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "discord-interactions": "^3.4.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
